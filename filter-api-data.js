function filterApiData(apiData, mandatoryKeys) {
  // was kommt rein?
  // apiData = array
  // [{ id: 1, price: 100 }, { price: 50 }];
  // mandatoryKeys = array
  // was kommt raus?
  // return = ein array
}
